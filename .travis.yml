
dist: xenial
sudo: required

script:
 - (cd build && cmake .. && make && ctest --output-on-failure)

matrix:
  include:
   - os: linux
     addons:
       apt:
         sources:
           - ubuntu-toolchain-r-test
         packages:
           - gcc-8 gfortran-8 python3
     install:
     before_install:
       - export FC=gfortran-8
       - echo $LD_LIBRARY_PATH
       # - cmake --help-module-list # quite outdated usually :-/
